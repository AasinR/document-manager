export enum UserPermission {
    ADMIN = "ADMIN",
    USER = "USER",
}

export enum GroupPermission {
    MEMBER = "MEMBER",
    ADMIN = "ADMIN",
    OWNER = "OWNER",
}

export enum LabelType {
    PUBLIC = "PUBLIC",
    PRIVATE = "PRIVATE",
    GROUP = "GROUP",
}
